/*编写一个C语言程序，通过十进制的赋值，设置程序断点，监视窗口和内存单元，查对上述作业题*/
#include <stdio.h>
int main(int argc, char *argv[])
{
	double a = -0.625;
	double b = -1000.5;
	double c = -7.5;
	double d = 306;
	printf("%f %f %f %f\n",a, b, c, d); 
	return 0;
} 
